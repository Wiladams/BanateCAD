

local lshape = shape_metaball:new({
		balls = {{-7, -12, 0, 5}, {8, -12, 0, 5}, {-2, 13, 0, 5}},
		radius = 200,
		stacksteps = 10,
		anglesteps = 10
		});

local verts = lshape:GetVertices();
for i,v in verts do
	translate(v)
	cube(0.25)
end