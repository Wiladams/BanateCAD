-- The offset determines how big the hollow in the center of the torus is
-- The first entry is the offset along the y-axis
-- the second entry is the offset along the x-axis
offset = {0, 10}

-- The size determines the size of the cross section of the torus
-- First entry is the 'height' in the z-axis
-- Second entry is the 'width' along the x-axis
size = {6,2}

usteps = 90	-- The is the resolution around the cross section
wsteps = 180	-- The resolution around the great ring


color(crayola.rgb("Black"))
torus({Offset=offset,
	Size=size,
	USteps = usteps,
	WSteps = wsteps,
	})

color(crayola.rgb("Red"))
translate({offset[2]*2+size[2]*2.618, 0, 0})
torus({Offset=offset,
	Size=size,
	USteps = usteps,
	WSteps = wsteps,
	})

color(crayola.rgb("Blue"))
translate({-offset[2]*2-size[2]*2.618, 0, 0})
torus({Offset=offset,
	Size=size,
	USteps = usteps,
	WSteps = wsteps,
	})


color(crayola.rgb("Yellow"))
translate({(-offset[2]*2-size[2]*2.618)/2, -offset[2], 0})
torus({Offset=offset,
	Size=size,
	USteps = usteps,
	WSteps = wsteps,
	})

color(crayola.rgb("Green"))
translate({(offset[2]*2+size[2]*2.618)/2, -offset[2], 0})
torus({Offset=offset,
	Size=size,
	USteps = usteps,
	WSteps = wsteps,
	})
